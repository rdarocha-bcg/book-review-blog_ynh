HOST=127.0.0.1
PORT=3000
NODE_ENV=development

DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=
DATABASE_NAME=book_review_blog

# Comma-separated YunoHost usernames with app admin role
ADMIN_USERNAMES=admin

# auto | always | never — trust YNH_*-style headers (SSOWat) from the client connection
TRUST_SSO_HEADERS=auto
