Angular-based book review blog with reviews listing, authentication (JWT), and admin panel (moderation, user management, statistics).

The frontend is built on the server during installation and served as a static single-page application. API requests are proxied to a configurable backend URL.
