test_format = 1.0

[default]
    args.domain = "domain.tld"
    args.path = "/blog"
    args.api_url = "http://localhost:3000"

[default.test_upgrade_from]

[multi]
    args.domain = "domain.tld"
    args.path = "/reviews"
    args.api_url = "http://localhost:3000"

[root_install]
    args.domain = "domain.tld"
    args.path = "/"
    args.api_url = "http://localhost:3000"
