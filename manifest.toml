#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/manifest.v2.schema.json

packaging_format = 2

id = "book-review-blog"
name = "Book Review Blog"
description.en = "A book review blog with reviews, YunoHost SSO authentication, and admin panel."
description.fr = "Un blog de critiques littéraires avec avis, authentification SSO YunoHost et panneau admin."

version = "0.1~ynh5"

maintainers = ["rdarocha-bcg"]

[upstream]
license = "Proprietary"
code = "https://github.com/rdarocha-bcg/book-review-blog_ynh"

[integration]
yunohost = ">= 12.0"
helpers_version = "2"
architectures = "all"
multi_instance = true
ldap = true
sso = true
disk = "500M"
ram.build = "512M"
ram.runtime = "200M"

[install]
    [install.domain]
    type = "domain"

    [install.path]
    type = "path"
    default = "/blog"

    [install.init_main_permission]
    type = "group"
    default = "visitors"

    [install.admin_usernames]
    type = "string"
    default = ""
    ask.en = "Comma-separated YunoHost usernames for application admins (e.g. alice,bob). Do not use dots as separators (e.g. not alice.bob). Semicolons or spaces also work. Leave empty if none."
    ask.fr = "Logins YunoHost des admins de l'app, séparés par des virgules (ex. alice,bob). Ne pas utiliser des points comme séparateurs (pas alice.bob). Points-virgules ou espaces possibles aussi. Laisser vide si aucun."

[resources]
    [resources.system_user]

    [resources.install_dir]

    [resources.database]
    type = "mysql"

    [resources.ports]
    main.default = 3000

    [resources.permissions]
    main.url = "/"
    main.show_tile = true

    admin.url = "/admin"
    admin.show_tile = false
    admin.allowed = "admins"

    [resources.apt]
    packages = "nginx"
